---
name: LeaveButton
route: /LeaveButton
menu: Non-page specific
---

import { Playground, Props } from 'docz';
import LeaveButton from './LeaveButton';
import 'rsuite/dist/styles/rsuite-default.min.css';

# LeaveButton

The `LeaveButton` component is used in the header to allow users to leave their platform.

<Props of={LeaveButton} />

It is normally used in conjunction with the [`Header`](/Header) wrapper component.

## Basic usage

<Playground>
  <LeaveButton />
</Playground>
